export function BafiSection() {
  return (
    <div className="p-4">
      <p className="text-sm text-gray-600">
        BAFI cross-check endpoints not exposed yet — only the underlying mirror is wired.
      </p>
    </div>
  );
}
